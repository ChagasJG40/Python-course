def somar(a = 0, b = 0, c = 0):
#def contador(i, f, p):
    """
    Faz a contagem e mostra na tela
    :param i: início da contagem.
    :param f: fim da contagem
    :param p: passo da contagem
    :return: sem retorno
    Função criada por George Chagas.
    """
    s = a + b + c
    print(f'A soma vale {s}')
    #c = i
    #while c <= f:
     #   print(f'{c}', end='')
      #  c += p
    #print('FIM')

somar(3, 5, 9)
somar(4, 8)
somar(9)
somar()
#help(contador)
